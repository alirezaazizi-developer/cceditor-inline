/*
* This script for validation user value
**/


/* function http validation */

function httpValidator(input) {
    let regex = /^((http|https)+)$/;
    // when matched return true
    // else return false
}
